#pragma once
#include"stdafx.h"
//Header-файлы НИКОГДА не компилируются
template<typename T>void Print(T arr[], const unsigned int n);
template<typename T>void Print(T** arr, const int rows, const int cols);